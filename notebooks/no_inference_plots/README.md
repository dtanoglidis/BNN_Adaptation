Here are plot of bnn trained on [training set], test on [testing set]. The names are structured `[chain number]_[training set]_[testing set]_[plot index].png`

These plots are produced by the jupyter notebooks.
